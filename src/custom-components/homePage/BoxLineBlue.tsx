import { Box, styled } from '@mui/material'

const BoxLineBlue = styled(Box)(
    ({ theme }) => (
        {
            backgroundColor: '#1c2235',
            width: '100%',
            height: '.5em',
            marginBottom: '20px'
        }
    )
)

export default BoxLineBlue